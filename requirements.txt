# CPU wheels: pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
torchvision>=0.15
numpy>=1.24
matplotlib>=3.7

# only needed to open/execute the notebooks
jupyter>=1.0
